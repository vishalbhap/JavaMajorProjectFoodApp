import { SearchRestAreaPipe } from './search-rest-area.pipe';

describe('SearchRestAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRestAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
